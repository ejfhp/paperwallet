var tabnames = ["gallery", "paperarea"]

function ShowTab(tabname) {
    tabnames.forEach( t => {
        let tab = document.getElementById(t);
        if (t == tabname) {
            tab.style.display="block"
            if t== paperarea {
                ninja.tab.
            }
        } else {
            tab.style.display="none"

        } 
    })
  }